#include <iostream>  // Include the input-output stream library for displaying output and handling input
using namespace std; // Allows us to use standard library objects and functions without a prefix (e.g., cout)

int main() // The main function is the entry point of the program where execution starts
{
    // Print the message "Hello, C++ World!" to the console
    cout << "Hello, C++ World!" << endl;

    return 0; // Return 0 to indicate that the program has executed successfully
}
